# Streamlit Cloud 配置示例
# 如果需要保存敏感信息（如 API 密钥），可以在此添加
# 在 Streamlit Cloud 的 App settings 中也可以配置这些值

# 例如：如果要保存默认的 OpenD 地址作为后备
# default_opend_host = "your.remote.server.ip"
# default_opend_port = 11111
